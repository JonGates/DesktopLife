# Insect configuration and fly motion

Implement fixed one fly, configurable cockroaches (0–500), ants (0–500), caterpillars (0–100). Preserve shared desktop topology and existing click-to-land timing. Migrate old JSON by ignoring the former fly count. Render the photographic fly as independently animated wing/body/foreleg layers at 60% of its former size; cache frames. Add ant walking and caterpillar undulation. Verify population invariants, boundaries, persistence, animation frames, build, and local published startup before pushing.
